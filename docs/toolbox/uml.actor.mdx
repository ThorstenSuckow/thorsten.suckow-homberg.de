---
title: "Actor"
---

import {BackButton} from "../../src/components/BackButton.js";
import {BibRef} from "../../src/components/References";

# Actor (UML)

An **Actor** is an entity that interacts with a system.
An **Actor** can be a person, or another system.

> _"**Actors** carry out [Use Cases](uml.usecase)."_ <BibRef name="UML" pp="99" />

import ActorsUml from './img/actor.uml.svg';
import {Embed} from "../../src/components/Embed";

<Embed
    width={640}
    figure={1}
    title={"The UML-notation for an Actor."}
>
    <ActorsUml />
</Embed>


<BackButton />